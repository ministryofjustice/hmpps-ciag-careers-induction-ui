<h2 class="govuk-heading-m">Things that may affect their ability to work</h2>

<dl class="govuk-summary-list govuk-!-margin-bottom-8">
  <div class="govuk-summary-list__row">
    <dt class="govuk-summary-list__key govuk-!-width-one-quarter">
      Potential affect on ability to work
    </dt>
    <dd class="govuk-summary-list__value">
      {% for item in record.abilityToWork %}
        <div>{{ contentLookup.fields.abilityToWork[item] }}{{ ' - ' + record.abilityToWorkOther if item === 'OTHER' }}{{ ',' if record.abilityToWork.length !== loop.index }}</div>
      {% endfor %}
    </dd>
    <dd class="govuk-summary-list__actions">
      <a class="govuk-link" href="{{ addressLookup.createPlan.abilityToWork(id, 'edit') }}">
        Change<span class="govuk-visually-hidden"> what may have an affect on ability to work</span>
      </a>
    </dd>
  </div>
</dl>